:root {
  --bg: #0b1220;
  --card: #111a2e;
  --text: #eaf0ff;
  --muted: #b8c4e6;
  --border: rgba(255,255,255,0.12);
  --accent: #f2c14e;
}

* { box-sizing: border-box; }
html, body { height: 100%; }
body {
  margin: 0;
  font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
  background: radial-gradient(1200px 600px at 30% -10%, rgba(242,193,78,0.25), transparent 60%),
              radial-gradient(900px 500px at 90% 10%, rgba(120,160,255,0.25), transparent 55%),
              var(--bg);
  color: var(--text);
}

.wrap { width: min(1100px, 92vw); margin: 0 auto; }

.site-header {
  padding: 28px 0 14px;
  border-bottom: 1px solid var(--border);
  background: rgba(0,0,0,0.15);
  backdrop-filter: blur(6px);
}
.site-header h1 { margin: 0 0 6px; font-size: 28px; }
.subtitle { margin: 0; color: var(--muted); }

.grid {
  display: grid;
  grid-template-columns: 360px 1fr;
  gap: 16px;
  padding: 16px 0 24px;
}
@media (max-width: 900px) {
  .grid { grid-template-columns: 1fr; }
}

.panel {
  background: rgba(17,26,46,0.85);
  border: 1px solid var(--border);
  border-radius: 16px;
  padding: 14px;
  box-shadow: 0 10px 30px rgba(0,0,0,0.25);
}
.panel h2 { margin: 0 0 10px; font-size: 18px; }

.search {
  width: 100%;
  padding: 10px 12px;
  border-radius: 12px;
  border: 1px solid var(--border);
  background: rgba(0,0,0,0.2);
  color: var(--text);
  outline: none;
}
.search::placeholder { color: rgba(234,240,255,0.6); }
.hint { margin: 10px 0 12px; color: var(--muted); font-size: 13px; }

.actions { display: flex; gap: 10px; margin: 10px 0 12px; flex-wrap: wrap; }

.btn {
  padding: 10px 12px;
  border-radius: 12px;
  border: 1px solid var(--border);
  background: rgba(242,193,78,0.18);
  color: var(--text);
  cursor: pointer;
  font-weight: 650;
}
.btn:hover { transform: translateY(-1px); }
.btn.ghost { background: rgba(0,0,0,0.20); }

.list { list-style: none; margin: 0; padding: 0; max-height: 520px; overflow: auto; }
.list li {
  border: 1px solid var(--border);
  border-radius: 14px;
  padding: 10px 10px;
  margin-bottom: 10px;
  background: rgba(0,0,0,0.18);
  cursor: pointer;
  transition: transform .06s ease;
}
.list li:hover { transform: translateY(-1px); }
.list .name { font-weight: 700; }
.list .meta { color: var(--muted); font-size: 13px; margin-top: 2px; }

.map-wrap {
  position: relative;
  background: rgba(17,26,46,0.85);
  border: 1px solid var(--border);
  border-radius: 16px;
  overflow: hidden;
  box-shadow: 0 10px 30px rgba(0,0,0,0.25);
}
.map { height: 640px; }
@media (max-width: 900px) { .map { height: 520px; } }

.status {
  position: absolute;
  left: 12px;
  bottom: 12px;
  padding: 8px 10px;
  border-radius: 12px;
  font-size: 13px;
  color: var(--muted);
  background: rgba(0,0,0,0.35);
  border: 1px solid var(--border);
  pointer-events: none;
}

.site-footer {
  border-top: 1px solid var(--border);
  padding: 16px 0 26px;
  color: var(--muted);
  background: rgba(0,0,0,0.12);
}
